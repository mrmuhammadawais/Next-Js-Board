"use client"; // Ensure this file is a client component
import React from 'react';
import TodoList from '../components/functional-components/todoList/page'; // Adjust the import based on your directory structure

const TodoListPage = () => {
  return <TodoList />; // Render the Todo List component
};

export default TodoListPage;
